# .specify - specs (reserved)

Reserved for feature specs and planning docs.

## Usage

Specs live here when using spec-driven workflow.
Each feature gets a subfolder with `spec.md`, `plan.md`, `tasks.md`.

This directory is kept with `.gitkeep` so the structure is preserved in git.
